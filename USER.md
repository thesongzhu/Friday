# Maintainer Preferences

- Conclusion first, details second.
- Prefer localized patches over broad refactors.
- Avoid rewriting large files when a targeted edit is enough.
- Keep repo-specific policy in workspace context files and reusable behavior in `skills/`.
- Update docs when behavior or operating conventions change.
- These are defaults only. A direct user request overrides them.
