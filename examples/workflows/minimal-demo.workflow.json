{
  "name": "Minimal Demo Workflow",
  "description": "A 2-node workflow that completes without external providers.",
  "tags": ["demo", "quickstart"],
  "graph": {
    "nodes": [
      {
        "id": "trigger",
        "type": "trigger",
        "label": "Manual Trigger",
        "config": {
          "triggerType": "manual"
        }
      },
      {
        "id": "result",
        "type": "data",
        "label": "Result",
        "config": {
          "mapping": {
            "message": "hello from friday demo"
          }
        }
      }
    ],
    "edges": [
      {
        "id": "e1",
        "sourceNodeId": "trigger",
        "targetNodeId": "result"
      }
    ]
  }
}
