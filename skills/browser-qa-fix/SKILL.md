# Browser QA Fix

Applies a bounded low-risk browser QA fix by reading page evidence and patching a single HTML entrypoint.

Typical triggers:

- `apply a browser qa fix`
- `fix the page title`
- `turn this browser qa report into a safe fix`
