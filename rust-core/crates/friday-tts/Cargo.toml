[package]
name = "friday-tts"
description = "Friday F11 net-new media capability — text-to-speech DI seam, OpenAI-compatible request shaping, unavailable default, and deterministic offline stub. Hub-only; no live executor is flipped by this crate."
edition.workspace = true
version.workspace = true
license.workspace = true
publish = false
rust-version.workspace = true

[dependencies]
serde_json = "1"
thiserror.workspace = true
