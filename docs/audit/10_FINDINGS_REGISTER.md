# Phase 11 - Findings Register

| ID | Severity | Category | Status | Affected Feature(s) | Evidence | Impact | Next Action | Blocks Launch | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | P0 | Security, Closed-loop wiring | RESOLVED | Marketplace | Active-scope grep finds no marketplace refs in `src ui scripts test package.json .env.example`. | Old paid entitlement self-grant risk removed with feature retirement. | Keep source/route contract hygiene to prevent reintroduction. | NO | High |
| F-002 | P1 | Auth/authz | RESOLVED | Local login | Active-scope grep finds no passwordless bypass refs; install/Docker/setup tests use passphrase. | Old no-password local login path removed. | Keep passphrase bootstrap tests. | NO | High |
| F-003 | P1 | Security | RESOLVED | Supply chain | `npm audit --audit-level=moderate --omit=dev` PASS after `axios` override. | Known axios advisory removed from npm production graph. | Track upstream SDK so override can be removed later. | NO | High |
| F-004 | P1 | Architecture | RESOLVED | Multi-tenant security (architecture boundary scaffolding only) | `npm run check:architecture-boundaries` PASS confirms no stray cross-layer imports between the security and rules layers; it does not certify cross-domain repository scoping or persistence-layer tenant isolation, which remain Phase 2 (RFC at `docs/architecture/multi-tenant-security-rfc.md`). | Security layer is architecturally separated from rules layer; this is necessary but not sufficient for tenant isolation, and the multi-tenant runtime path (env-gated by `FRIDAY_MULTI_TENANT_ENABLED=true`) is in-memory only. | Keep check in CI; do not treat this row as evidence that multi-tenant isolation is implemented. | NO | High |
| F-005 | P1 | Testing | RESOLVED | Full suite | `npm test` PASS: 10301 tests passed, 251 skipped, type errors 0. | Local test suite is green. | Keep live skipped lanes tracked separately. | NO | High |
| F-006 | P1 | Production readiness | RESOLVED | Docker/install auth | Docker clean passphrase smoke PASS; install smoke PASS. | Container/package auth path works without passwordless. | Keep unique-port smoke and token length checks. | NO | High |
| F-007 | P1 | Testing, Closed-loop wiring | RESOLVED | Real-world smoke (2026-05-01 SHA) | Reports `2026-05-01T21-21-24-003Z-fresh` and `2026-05-01T21-26-47-671Z-current-config` were captured locally on 2026-05-01 as 27/27 with `auth.source=local_passphrase_login`, DeepSeek primary, and OpenAI fallback. **2026-05-11 audit supplement**: the report files are not present in the current workspace at HEAD `9c27f9e98c1285818482e0dfe9a17ecb33aeb9c6` (`find -name "*2026-05-01T21-2*"` returns zero matches). The captured evidence is `local real-runtime/provider <deepseek+openai>` per `docs/release-evidence-policy.md`; it is historical/local diagnostic evidence only, **not** same-SHA release proof (the captured SHA predates 9 merged PRs #195-#203) and **not** RGG release proof (RGG artifact for current main is `status: blocked_by_env`). | Real LLM/tool/UI local smoke was captured for the 2026-05-01 SHA at a `local real-runtime/provider` tier; per §23.6 hard rule, that evidence is never same-SHA release proof on its own and is now historical relative to current main. The phrase "release-green" is not a defined tier in `docs/release-evidence-policy.md` and must not be reintroduced. | Keep these scenarios in CI/manual release gates and avoid passwordless/default-port regressions. Recapture against the current main SHA when a fresh local smoke run is performed; do not treat the 2026-05-01 capture as same-SHA proof. | NO | High |
| F-008 | P1 | Channels | OPEN | Live channels/Discord | Live channel suites skipped; no safe sandbox recipient/channel env in process; `DISCORD_BOT_TOKEN` is absent from the local/GitHub environment and the pasted token should be rotated. | Claimed channel delivery remains unverified. | Rotate pasted token, configure sandbox guild/channel IDs and token as environment secrets, run live channel E2E. | DEPENDS | High |
| F-009 | P1 | Production readiness | PARTIAL | External deployment | `fly.toml` now provides a Fly staging route, `staging-e2e` environment exists, and Cloud Live E2E has actor/ref guards; no Fly token/app/deployed URL is available yet. | Real CORS/cookie/OAuth/webhook deployment behavior remains unknown until staging is deployed. | Create Fly app, set secrets, deploy, then run Cloud Live E2E plus CORS/callback smoke. | YES for external launch | High |
| F-010 | P2 | Maintainability | OPEN | Hub/runtime architecture | Large composition/runtime files remain. | Future breakage/security review risk. | Incrementally split lifecycle modules and enforce warning budget. | DEPENDS | Medium |
| F-011 | P2 | Observability | OPEN | Audit trail lifecycle | Expected tests log DB-closed audit append failures. | Shutdown/late-write behavior needs hardening. | Add lifecycle drains and unexpected-late-write tests. | DEPENDS | Medium |
| F-012 | P2 | Workspace hygiene | RESOLVED | Local verification | 30 untracked duplicate/local artifact files were quarantined at `/tmp/friday-audit-quarantine-20260501T220523Z/`; repo-root `npm run check:migrations` now PASS. | Dirty workspace risk removed for this branch. | Keep generated/duplicate artifacts out of repo worktrees. | NO | High |
| F-013 | P2 | CI/CD governance | RESOLVED | Branch protection | GitHub API reports `strict=true` required checks, 1 required approval, conversation resolution enabled, force-push/delete disabled. **2026-05-11 audit supplement**: `gh api repos/thesongzhu/Friday/branches/main/protection` also reports `enforce_admins.enabled: false` and `dismiss_stale_reviews: true`; required approving review count is `1`. With `enforce_admins=false`, repo admins can implicitly bypass the 1-review requirement at merge time (no explicit `--admin` flag needed). PR #203 merge fact: `merged_by: thesongzhu`, `reviews: []` (0 approving reviews), `reviewDecision: REVIEW_REQUIRED`, `mergedAt: 2026-05-11T21:22:27Z`, merge commit `9c27f9e98c1285818482e0dfe9a17ecb33aeb9c6` — the merge was platform-allowed only because of the admin bypass mechanism above. This is a governance/process drift relative to §23.6 hard rule "`--admin` bypass of branch protection is forbidden by default" and §23.2 Stage 8 "PR author cannot self-approve", not a code-behavior regression. | Main branch is configured correctly for the 1-review-required path. Admin bypass remains platform-enabled because `enforce_admins=false`. PR #203 doc-only merge was performed under the implicit `enforce_admins=false` bypass (not an explicit `--admin` flag); for safety/security/release-governance scope, a non-admin reviewer path or `enforce_admins=true` is needed to make §23.6 enforceable at the platform layer. | Keep protection aligned with CI job names when workflows change. Decision needed (not in this slice): flip `enforce_admins.enabled` to `true` for §23.6 platform enforcement, or route admin-author PRs through a non-admin reviewer. Do not change branch protection or GitHub settings as part of this finding entry. | NO | High |
| F-014 | P2 | Observability | OPEN | External metrics/traces | Friday has internal observability routes/tests, but no configured OTEL/Grafana endpoint and RFC marks OTLP export as future work. | External alerts/traces cannot be claimed in production. | Implement/configure OTLP export or collector bridge, then verify events land in Grafana Cloud. | DEPENDS | Medium |
| F-015 | P2 | CI/CD governance | RESOLVED | Cloud Live E2E | `.github/workflows/cloud-e2e.yml:47-76` defines an `authorize-live-run` job that checks actor permission tier (admin/maintain/write) via `gh api repos/${REPOSITORY}/collaborators/${ACTOR}/permission` and pins ref to `branch=main`; both `cloud-contract` and `cloud-live-e2e` jobs declare `needs: authorize-live-run`. | In-workflow actor/ref check is now wired in addition to environment branch policy and GitHub permissions. | Keep the guard aligned if the workflow is renamed or split. | NO | High |
| F-016 | P2 | Architecture | OPEN | Packaging — architecture-vs-runtime honesty | Packaging engine uses in-memory `Map` storage (`src/packaging/engine/registry-manager.ts:180-184`, `src/packaging/engine/package-installer.ts:279-287`); bootstrap is env-gated by `FRIDAY_PACKAGING_ENABLED=true` (`src/hub/friday-hub-bootstrap.ts:5582`); no SQLite migration references package registry tables; bootstrap publish handler uses stub manifest data (`src/hub/friday-hub-bootstrap.ts:5605-5648`). | Packaging is Phase 1 in-memory preview — does not match the SQLite-backed registry, install-lifecycle persistence, trusted-key persistence, or real signature verification described in `docs/architecture/agent-package-rfc.md`. | Keep docs honest now (this row + RFC + source-of-truth + phase3-refined-ac caveats); Phase 2 requires SQLite-backed registry, install, lifecycle, trusted-key persistence, and real signature verification wiring. | DEPENDS | Medium |
| F-017 | P1 | Skills lifecycle / Approval boundary / Runtime wiring | OPEN | Skill lifecycle HTTP routes (`/v1/skills/catalog`, `/v1/skills/:skillId`, `/v1/skills/validate-manifest`, `/v1/skills/:skillId/verify`, lifecycle-branch `install`/`update`/`delete`); `friday-self-healing-live` skill-verification-drift `it` | Skill lifecycle routes are gated on `deps.lifecycle` (`src/api/http/routes/friday-skill-routes.ts:319`); hub does not pass `skillLifecycle` (`rg "skillLifecycle" src/hub/friday-hub-bootstrap.ts` returns zero matches); `createFridaySkillLifecycleService` has no callers outside its own definition (`src/skills/services/friday-skill-lifecycle-service.ts:880`) and re-export (`src/skills/index.ts:223`); `POST /v1/skills/:skillId/verify` returned route-level HTTP 404 (`No route matches`, from `src/api/http/friday-http-server.ts:547`) in the 2026-05-11 DeepSeek local live run of `test/e2e/live/friday-self-healing-live.e2e.test.ts:1027`; the lifecycle service `install/update/deleteSkill/verifySkill` paths (`src/skills/services/friday-skill-lifecycle-service.ts:1277-1559`) contain zero `assertCanonicalApproval` calls; only `PATCH /v1/skills/:skillId/content` enforces canonical approval today (`src/api/http/routes/friday-skill-routes.ts:514`). Documentation drift: `docs/current-source-of-truth.md` previously stated `/v1/skills/*` is the canonical lifecycle surface "as if fully wired" while `docs/audit/CAPABILITY_PROOF_MATRIX_2026-05-11.md:156` classified lifecycle as `mock-only`. The doc-honesty mirror is landing alongside this finding. | Skill verification drift self-healing path cannot be claimed closed; the `friday-self-healing-live` 5th `it` cannot pass against the real-runtime HTTP route in standalone hub; naïve `skillLifecycle` wiring would expose real `update`/`delete` mutating routes without canonical approval and is a §3 ask-before-act safety-boundary risk. The 2026-05-11 DeepSeek local run is partial real-runtime evidence for the auto-fix + lesson + rollback + anti-learning + workflow-failure paths only; it is not full self-healing closure, and it is not RGG release proof. | (1) Doc-honesty slice (landing with this finding) — align source-of-truth, capability matrix, and findings register; no code change. (2) Separate slice — add canonical approval gates for `update` and `delete` at route or service layer (parallel to PATCH content gate); high-risk, requires §3 ask-before-act + 2 isolated reviewers + Codex cross-check. (3) Separate slice — wire `createFridaySkillLifecycleService` in `createFridayApiRuntime`; depends on (2) landing first. (4) Test hygiene independent slice — move `friday-self-healing-live` skill-drift writes off `<repo>/skills/` (git-tracked production dir) into `os.tmpdir()` (see F-020 for standalone tracking). (5) After (2)–(4) land, rerun the skill-verification-drift `it` and confirm 200 with `evidence.dependencyCheck.missingBins`. | DEPENDS for any "skill lifecycle default-on" / "skill-verify self-healing closed-loop" / "full local real-runtime self-healing proof" claim. | High |
| F-018 | P2 | Test harness / capability gating | OPEN | `test/e2e/live/friday-autonomous-restart.e2e.test.ts` 4th `it` (visual verification); DeepSeek lane vs vision capability | The 4th `it` block at `test/e2e/live/friday-autonomous-restart.e2e.test.ts:298-388` declares `goalDescription` at line 320 containing "Use only the browser tool, not any desktop tool" and "verify visually that the Friday login page is shown". The autonomous engine routes verifying steps to `method: "llm_vision"` when images are present (`src/agent/autonomous/friday-autonomous-engine.ts:1495` `if (images.length > 0)`; `1517` `method: "llm_vision"`; fallback at `1550` stamps `method: images.length > 0 ? "llm_vision" : "llm_text"`). DeepSeek provider templates (`src/providers/model/friday-provider-templates.ts:176-188`) list only text/reasoning models (`deepseek-v4-pro`, `deepseek-v4-flash`, `deepseek-chat`, `deepseek-reasoner`); `grep -n "vision"` against the file returns zero matches. Deep-proof env (`test/e2e/live/_helpers/deep-proof-env.ts:260`) sets `FRIDAY_DEEP_PROOF_MODEL = FAST_MODEL` with no vision routing override. In the 2026-05-11 DeepSeek queue run, this test was excluded because the lane cannot satisfy the `vision` capability the test exercises. `docs/audit/CAPABILITY_PROOF_MATRIX_2026-05-11.md:126` currently tabulates `Autonomous restart matrix` as `local real-runtime + real-provider <selected lane>` for all four deep-proof lanes (anthropic/deepseek/openai/ollama), which is provider-agnostic overclaim for DeepSeek specifically. | The matrix/provider-agnostic claim is overbroad for the DeepSeek lane on `friday-autonomous-restart` because that test's 4th `it` requires vision-capable verification. Any DeepSeek lane run that reaches the visual-verification step will fail at the engine's `llm_vision` routing rather than producing real-provider evidence for the test. | (1) Doc caveat: in the new post-#203 capability matrix snapshot, annotate `Autonomous restart matrix` row with a per-lane caveat that DeepSeek lane cannot satisfy the 4th `it` visual verification. (2) Test-harness slice (independent, low risk): add a capability-gate `it.skipIf(...)` for the visual-verification `it` keyed on lane capability, or split it into a vision-only sub-test that DeepSeek skips. (3) Long-term: if a vision-capable DeepSeek model becomes available, register `vision` capability on the provider template and re-enable the test. Do not silently skip without a capability gate. | DEPENDS for any "provider-agnostic autonomous-restart" / "DeepSeek closes all deep-proof lanes" claim. | High |
| F-019 | P1 | Skills lifecycle / Generator-to-candidate-store bridge | OPEN | `test/e2e/live/friday-generator-maintenance-live.e2e.test.ts`; skill autonomy upgrade lifecycle | The generator approve endpoint at `src/api/http/routes/friday-skill-generator-routes.ts:879-929` returns envelope `{ skillId, skillDir, registryRefreshed, promotionStage, evidence }`; the test envelope at `test/e2e/live/friday-generator-maintenance-live.e2e.test.ts:73-93` mirrors that shape — no `candidateId`, no `canonicalApproval`, no `planDigest`. The `approveAndSave` implementation at `src/skills/generator/services/friday-skill-generator-service.ts:2021-2266` directly writes manifest + UI schema, calls `deps.memoryStateService.updateSkillStatus(skillId, "installed")` (line ~2203), `deps.registry.refresh()` (~2220), and `skillRepo.upsertSkillFromCatalog(...)` (~2228); `grep` for `skillUpgradeLifecycle` / `registerShadowVersion` / `registerCandidate` / `candidateId` / `canonicalApproval` / `planDigest` against this service file returns zero matches. By contrast, `src/autonomy/services/friday-skill-upgrade-lifecycle-service.ts` requires `candidateId` for every lifecycle action (detect / adapt / shadow / canary / promote / rollback) with 60+ `candidateId` references; `requireCandidate` (~lines 293-300) throws `SKILL_CANDIDATE_NOT_FOUND` for any skill not in the candidate store. Generator-origin skills bypass the candidate store entirely. | `friday-generator-maintenance-live.e2e.test.ts` cannot honestly run on the current contract because the generator approve flow does not produce a `FridayExternalSkillCandidate` row that the autonomy upgrade lifecycle can consume. The test was excluded from the 2026-05-11 DeepSeek queue for this reason. Two parallel skill lifecycles coexist (generator path vs. autonomy upgrade path) and the bridge between them is missing; "skill generator end-to-end closed loop" or "generator-to-autonomy unified lifecycle" claims are overclaim. | (1) Doc caveat: capability matrix `Generator maintenance (skill)` row notes the bridge gap. (2) Product design slice (high-risk, sibling of F-017 wiring): either generator approve emits a `FridayExternalSkillCandidate` row consumable by the autonomy lifecycle, or the autonomy lifecycle is extended to accept non-candidate origin inputs; requires §3 ask-before-act + 2 isolated reviewers + Codex cross-check. (3) After bridge lands, rewrite/split `friday-generator-maintenance-live.e2e.test.ts` so its assertions match the unified contract; rerun in DeepSeek queue. | DEPENDS for "skill generator end-to-end closed-loop" / "generator-to-autonomy unified lifecycle" claims. | High |
| F-020 | P2 | Test hygiene / workspace pollution risk | OPEN | `test/e2e/live/friday-self-healing-live.e2e.test.ts` skill-verification-drift `it` | `test/e2e/live/friday-self-healing-live.e2e.test.ts:37` declares `const BUNDLED_SKILLS_DIR = path.join(process.cwd(), "skills")`. `/Users/jarvis/Projects/Friday/skills/` contains 177 files tracked by git (`git ls-files skills/ | wc -l = 177`, production starter skills); `.gitignore` does not exclude `skills/` (`grep -nE "^skills\|/skills" /Users/jarvis/Projects/Friday/.gitignore` returns zero matches). The drift `it` writes the broken manifest to disk at line ~1007 (`fs.writeFileSync(manifestPath, JSON.stringify(brokenManifest, null, 2), "utf8")`) **before** the `try { ... }` block (starts ~1010); the `finally` cleanup (~1065-1069) restores the original manifest and `removeBundledSkill(skillId)`, but `finally` only runs if execution reaches inside `try`. If the test process is killed between line 1007 and line 1010 (e.g. timeout, SIGTERM, or `env.hub!.skills.refresh()` at line 1008 hangs and is killed by Vitest's 120_000 ms hard timeout at line 1071), the bundled production directory is left with a tampered manifest and no restoration, plus a leftover `e2e-skill-drift-<timestamp>` subdir under `<repo>/skills/`. This is independent of F-017 wiring and would still need fixing even if F-017 were resolved tomorrow. | A failed or killed live test can dirty production-tracked skill fixtures and leave the worktree with a corrupted bundled manifest. Routine `git status` cleanup is not sufficient because the corruption can be inside an already-tracked file rather than as an untracked artifact. | Independent test-only slice (low risk): move temporary skill writes to an `os.tmpdir()`-based directory with `beforeAll`/`afterAll` cleanup; do not write to `<repo>/skills/`; capability-gate the test from running until cleanup path is reachable. Do not modify product code. Coordinate with F-017 Next Action (4) but ship F-020 independently — it is hygiene, not wiring. | NO | High |
