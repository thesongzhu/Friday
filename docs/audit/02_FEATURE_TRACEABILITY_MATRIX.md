# Phase 2 - Feature Traceability Matrix

Status values: VERIFIED_CLOSED_LOOP, PARTIAL, UNWIRED, FAKE_OR_MOCK_ONLY, BROKEN, UNKNOWN.

| Feature | Entrypoints | Backend/Data Trace | Tests/Verification | Status | Evidence/Risk/Next Action |
| --- | --- | --- | --- | --- | --- |
| Health/version/static UI | `/v1/health`, `/v1/version`, `/` | HTTP server routes, static `dist/ui` | `npm run build:ui` PASS; `test/e2e/api/friday-api-health-routes.test.ts` PASS; CLI start tests PASS | VERIFIED_CLOSED_LOOP | Good local smoke evidence. Add deployed health check verification. |
| Auth/bootstrap/local session | UI auth provider, `/v1/auth/*` | auth service, session/token DB, RBAC policy | Auth route/unit tests PASS; remote IP bypass rejection PASS | PARTIAL | Local bypass is default true in `src/hub/friday-hub-bootstrap.ts:698-702`; `.env.example:108-109` documents it as true. Production exposure remains high risk. |
| Provider setup/routing | Setup UI, provider routes | provider service, secret repository, routing config | provider lifecycle tests PASS; real-world smoke found DeepSeek provider lane | PARTIAL | `validate:real-world:smoke` found a default DeepSeek `deepseek-v4-flash` lane with healthy validation, but no fallback lane; local closure provider lifecycle failed on invalid OpenAI test key. |
| Agent chat/sessions | `/chat`, `/agent`, `/v1/agent/runs`, `/v1/sessions/*` | agent runtime, session repositories, memory/world model | mock E2E PASS; session integration PASS; real-world smoke PARTIAL | PARTIAL | Real-world smoke passed some DeepSeek-backed L3 direct answer/summary/json scenarios, but failed multi-turn memory and file-tool roundtrip. Browser stores chat history locally, which can mask persistence failures. |
| Workflows CRUD/run/approval/triggers | Workflow pages, `/v1/workflows/*`, workflow builder | workflow runtime, run/checkpoint/approval/trigger tables | workflow lifecycle/approval/trigger integration tests PASS | PARTIAL | Strong local integration evidence, but UI browser flows skipped and production trigger/webhook deployment not verified. |
| Workflow public webhooks | public `/v1/workflow-webhooks/:pathToken` | workflow trigger runtime and secret resolver | trigger integration tests PASS | PARTIAL | Public route depends on secret resolver and deployment config; no external webhook smoke. |
| Automations/scheduler | Automations UI, scheduler jobs | agent automation tables, scheduler | mock journey automation lifecycle PASS; scheduler tests PASS | PARTIAL | Mostly mock/local; no production double-submit or delayed job smoke. |
| Memory | Memory UI, `/v1/memory/*`, session extraction | memory items/embeddings/FTS, guard/quota/PII | memory pipeline/guard tests PASS | PARTIAL | Embedding fallback logged when no model routing configured; semantic path not live-verified. |
| Channels | Channels UI, channel webhooks | channel services, relay, session bridge | channel unit tests PASS; live channel tests skipped | FAKE_OR_MOCK_ONLY | WhatsApp/Signal/IRC/LINE service files say stubbed; Slack returns stub user; Discord outbound can return stub IDs. Need real sandbox channel verification. |
| Marketplace requests/assets/catalog | Marketplace UI, `/v1/marketplace/*` | marketplace repositories/cache/source/request services | request/source/cache/service tests PASS | PARTIAL | Catalog/request mechanics have tests; not all UI paths browser-verified. |
| Marketplace paid checkout/entitlements | `/v1/marketplace/checkout`, `/v1/marketplace/purchases/:id/complete` | purchases, entitlements, pricing | duplicate callback test PASS, purchase-manager unit PASS | BROKEN | `complete` route is authenticated with `marketplace.write` and grants entitlement after tenant access check, without requiring verified provider callback (`src/api/http/routes/friday-marketplace-commerce-routes.ts:868-927`). |
| Billing webhooks | expected by billing domain/types | billing handler, adapter registry, billing events | billing handler unit PASS | UNWIRED | No registered API route found for provider billing webhooks; handler exists in `src/marketplace/billing/friday-billing-webhook-handler.ts`. |
| Plugins | Plugins UI, `/v1/plugins/*` | plugin registry/loader/repository | plugin unit tests PASS; API plugins e2e PASS | PARTIAL | UI can show stub runtime (`ui/src/routes/plugins-page.tsx:196,338`); browser E2E mostly skipped. |
| Skills install/run/generation | Skills UI, skill routes, generator routes | skill registry/executor/generator/marketplace | executor/generator/install tests PASS | PARTIAL | API skills E2E uses mock converter; live install/use paths need staging proof. |
| Desktop/system companion | System/Desktop UI/routes | macOS Swift companion, remote passkeys/sessions, sockets | desktop route/unit tests PASS; native companion tests FAIL | BROKEN | Native companion release/runtime failed with timeouts/signing/lock failures in `npm test`. |
| Observability/audit | Observability UI, observability routes | trace/metric/audit/SLO/alert tables | unit route/service tests PASS with expected failures | PARTIAL | Test output includes `OBS_AUDIT_APPEND_FAILED` and DB closed errors. Need lifecycle/flush hardening. |
| Multi-tenant security | conditional routes, policies | multi-tenant engines/tables | unit tests PASS; architecture check FAIL | PARTIAL | Feature disabled by default and `check:architecture-boundaries` fails on security import escape. |
