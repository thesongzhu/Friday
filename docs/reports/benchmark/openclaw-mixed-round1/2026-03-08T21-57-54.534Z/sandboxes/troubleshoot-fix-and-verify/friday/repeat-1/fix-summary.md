### Fix Summary

The function `add(a, b)` in `calc.js` was corrected from performing subtraction (`return a - b;`) to performing addition (`return a + b;`). The verification command `node check.mjs` passed successfully after this change.